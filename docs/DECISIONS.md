# Locked decisions

**Version:** 0.10.0

All notable locked product/tech decisions. Through **D46** for the v0.6 product line; **v0.7.0** added Power Search, continue-then-review bulk ops (D18 amended), per-tab search (D29), PowerPoint slide preview, folder-statistics depth-first tagging, slideshow crop, and nested custom context submenus (no new D-number). **v0.8.0** adds drive free space, detached preview, `.ics`/`.eml` preview, and **D49** (session-only remote listing memory cache). **v0.8.1** added **D50** opt-in media metadata — [MEDIA_METADATA.md](MEDIA_METADATA.md). **v0.9.0** ships **D51** as a universal local script runner + optional AI authoring — [SCRIPTS.md](SCRIPTS.md). **v0.10.0** ships **D52** — drive-only NTFS USN journal manager from Drive Properties.

Change only with an explicit decision update. Prefer amending this table over silent drift.

| ID  | Decision                                                                                                                                                    | Why                                                                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| D1  | **Windows-first** Electron desktop app. Experimental Linux AppImage / Wayland helpers exist for contributors ([LINUX.md](LINUX.md)) but are **not** a supported product matrix; Win32 features degrade/no-op off Windows | Matches Explorer replacement use case; keep Linux from blocking Windows releases |
| D2  | **All app state in Electron `userData`** — never write MyFileExplorer sidecars into browsed folders                                                         | User folders stay clean; backup/app reset is clear                      |
| D3  | **Tabs** are the primary navigation unit; restore session on launch. Unreachable paths (unmounted / encrypted drives) stay open as **Offline** and auto-retry until available — never drop session tabs just because a volume is offline. The tree **Drives** list is live mounts (USB eject removes the letter). **Mapped network letters** remain listed when disconnected (Explorer red-X); opening them reconnects via **WNetAddConnection2W** (interactive `WNetUseConnectionW` when credentials are needed). **Disconnect N:** on a mapped letter runs **WNetCancelConnection2W** + **CONNECT_UPDATE_PROFILE** (forget persistent mapping; force if in use). `WNetRestoreConnectionW` is not relied on — often missing from `mpr.dll` | Multi-folder workflows; reboot + late-mounted volumes must not lose tabs; tree matches Explorer; ditch WFE for mapped-drive reconnect / forget |
| D4  | **Curated context menu** — short allowlist, not Explorer’s full verb dump. Extended by **D41** (user-defined external commands + optional hide of built-in verbs). Includes **Open Command Line here** on folders (tree, file list, empty pane) via `shell:openCommandLine` — **ShellExecute** to **cmd.exe** or **powershell.exe** (`commandLineShell` in Settings → Behavior; default cmd). Click → current user (`open`). **Shift+click** (or Shift held) → **as administrator** (`runas` / UAC). Not Windows Terminal. Tree **Drives** header (This PC): **Computer Manager** (Explorer Manage / Computer Management), **Device Manager**, **Control Panel**, and **Properties** (This PC property sheet) via `shell:openWindowsTool` — real Windows windows, not in-app UI | Speed and clarity; ditch WFE for a console in the folder; This PC admin MMC/CPL without leaving the tree |
| D5  | Preview shows **A1111 / ComfyUI metadata when parseable** from common embeddings (PNG tEXt/iTXt etc.); omit rows if absent                                  | Core user need for AI image libraries                                   |
| D6  | Search acceleration via **opt-in indexed roots** + SQLite under userData (FTS5 when available; LIKE primary for substring / Everything predicates). Extended by **D34** (hybrid folder + volume) | Fast search where it matters; no mandatory whole-disk index             |
| D7  | **Del** → Windows Recycle Bin. Implementation: Electron `shell.trashItem` (IFileOperation) first, then `SHFileOperation` + `FOF_ALLOWUNDO`, then VisualBasic `SendToRecycleBin`. Refuse Del on volumes that cannot recycle. Directory watches are suspended for the duration so Chromium/Node cannot re-arm `ReadDirectoryChanges` mid-op. **Preview/media**: never `file://` to browsed paths. `mfe-media` buffers ≤128 MiB (source closed); larger non-AV uses userData scratch; **AV/PDF use HTTP byte-range (206)** so Chromium can play/seek — ≤128 MiB from memory, larger AV ranges the allowlisted path directly (no multi‑GB scratch). **Shift+Del** → permanent unlink with confirm when multi-select or directories. Per-item trash/delete failures **continue** and join the end-of-op review (D18) instead of aborting the rest. **In-app Recycle Bin** (tab-bar button): list bin contents in the normal file view (like search), Restore (Enter / context / banner), Empty Recycle Bin (`SHEmptyRecycleBin`, no COM item walk), Del permanently removes from the bin — never opens system Explorer. Two deletes of the same original path are two rows (`recyclePath` identity, Date deleted differs), like Explorer; Ctrl+Z after Del restores the newest of that path only | Spec-accurate recycle; avoid silent permanent deletes; never require Explorer |
| D8  | Themes = **CSS variables**; modes dark / light / custom token map; font family + size persisted                                                             | Comfort without theme-engine bloat                                      |
| D9  | **Typed preload IPC** + **Zod** validation; Result `{ ok, value \| error }` envelope                                                                        | Same reliability pattern as modern Electron apps                        |
| D10 | Thumbnails cached under userData keyed by **path + mtime + size**                                                                                           | Correct invalidation without hashing file bytes                         |
| D11 | Drag-drop: **Windows Explorer modifier conventions** (Ctrl=copy, default move same volume, etc.). **Right-button drag** opens a drop menu (Copy here / Move here / **Create shortcuts here** / Cancel) like Explorer — shortcuts are `.lnk` via WScript (works for `.exe` / `.bat` / `.lnk` / folders / any file). **Left-drag**: pointer gesture for in-app move/copy onto folders/tree/**tabs** (drop on a tab → that tab’s folder); when the pointer leaves the BrowserWindow, sync `webContents.startDrag` exports CF_HDROP to other apps. (Calling `startDrag` from HTML5 `dragstart` on Windows blocks in-app drops.) **Cancel**: Escape, or the **opposite mouse button** (left-drag → right cancels; right-drag → left cancels), suppressing the leftover context menu / click. **Edge auto-scroll**: while a left/right drag is active, hovering near the top/bottom of the file list or folder tree scrolls that pane (Explorer parity). **Tree drag-hover expand**: hovering a collapsed folder ~2s expands it so the drop can continue into a child | Muscle memory; Explorer replacement must drag into other apps without breaking folder drops; tabs as sort categories |
| D12 | Renderer has **no Node integration**; filesystem only through main                                                                                          | Security                                                                |
| D13 | Default tab title = **folder name**; custom title optional and sticky until cleared. Optional **Lucide** tab icon + color (D32) | Renamed / labeled tabs for long-lived workspaces |
| D14 | Preview pane and splitter sizes are **session chrome** (persist in `session.json`). Docked pane chrome and the detached preview window are independent. | Restore layout                                                          |
| D15 | Unindexed search is allowed but must show **progress + cancel** and never claim indexed speed                                                               | Honest UX                                                               |
| D16 | Search DB uses Node's built-in **`node:sqlite`** (Electron ≥ 35) instead of `better-sqlite3`; FTS5 detected at runtime with **LIKE fallback**               | No native rebuild toolchain required; same schema either way            |
| D17 | **`userData` is always `%APPDATA%\MyFileExplorer`** for both `npm run dev` and installed builds. Optional `MFE_ISOLATED_USER_DATA=1` → repo `.dev-user-data/`; `MFE_USER_DATA` overrides the path. One-time migrate from legacy `.dev-user-data` / `my-file-explorer` when those profile files are newer | Same prefs while developing and after install; reinstall must not feel like a reset |
| D18 | Bulk copy/move/trash/delete **never pause the first pass** for per-item input. Process every auto-ok item; queue `OpIssue`s (`name_conflict`, `busy`, `not_allowed`, `not_found`, `path_too_long`, `io`, `fatal`). No blocking pre-transfer `checkConflicts` prompt. When the pass finishes, one **review** UI groups similar issues with apply-to-similar (Skip / Keep both / Replace / **Keep most recent** / Retry). Name-conflict rows still use side-by-side compare cards (lazy `fs:checkConflicts` when focused). Same-folder copy-paste stays auto **Keep both** (`name (2).ext` …) with no dialog. **Rename** (F2 / Power Rename) uses the same review when the new name already exists — Keep both is `name (2).ext`. Fatal dest (disk full / destination gone) **stops remaining work to that destination** but still opens review. Cancel stops the first pass; queued issues so far still review. Second pass is `fs:resolveIssues` (`copy` \| `move` \| `trash` \| `delete` \| `rename`). | Sparse problems must not stall a million-file copy; smarter than mid-op “do this for all” |
| D19 | **Single-instance** app; CLI `--reveal`/`--open` and `mfe://` protocol forward into the running process as a tab (file → parent + selection) | Lets other apps replace “Reveal in Explorer” without spawning Explorer |
| D20 | Quick access defaults to **Desktop / Downloads / Documents / Pictures** (not Home alone); user can pin/unpin folders; state in settings | Home-only was wasted space; Explorer-familiar shortcuts |
| D21 | File/folder **icons come from the Windows shell** via `SHGetFileInfo` (koffi) on win32, with `app.getFileIcon` fallback; cached under userData. Content thumbs stay for images/PSD; videos may use `!VIDTHUMB_CACHE` strips (D26). Folder cache keys include `desktop.ini` mtime so Dropbox/custom folder icons refresh. **Extract is serialized** on main (SHGetFileInfo is sync) with yields between jobs; renderer throttles concurrent per-file (`.exe`/`.lnk`/…) `icons:get` so large app folders stay interactive | Explorer-accurate special folders + desktop.ini; Electron’s API alone collapses most folders to one glyph |
| D22 | **Per-folder view overrides** in settings (`folderViews`): view mode, sort, Details columns; optional **recursive** scope. Exact match wins, else longest recursive ancestor. Does not lock the tab — non-matching paths keep the tab’s baseline view + global columns | AI libraries and media folders need different layouts without sticky tab modes |
| D23 | **In-app undo/redo** (Ctrl+Z / Ctrl+Y) for trash, move, copy, rename, **Power Rename** (multi-pair), and new file/folder. Stack is session-memory only (max 30). Trash undo restores via Recycle Bin; permanent delete is not undoable | Explorer muscle memory; no dependency on system-wide Explorer undo |
| D24 | Per-tab **folder-tree expand/collapse** persists in `session.json` as `treeExpanded` (path list, capped). With path, selection, scroll, view/sort, and chrome splitters, relaunch restores the same workspace view | Exact “leave and come back” continuity |
| D25 | **Named workspace layouts** in settings (`layouts[]`): snapshot of tabs (path, title, icon, view/sort, rootPath, treeExpanded) + active tab index + chrome splitters + multi-view fields (D31: `viewLayout`, pane tab indexes, pane split ratios). Apply replaces the live tab set (new tab ids; history/selection/scroll cleared). Orthogonal to per-folder `folderViews` (D22). Cap 50. Manage via toolbar Layouts menu and Settings → Layouts | Task-specific workspaces (AI training, book editing, project coding) without juggling one session |
| D26 | **Video icon thumbs** from a sibling hidden `!VIDTHUMB_CACHE` folder: `{videoName}.thumb_1.jpg`…`thumb_20.jpg`, looped in icon/thumbnail views. Frame delay is settings `vidThumbFrameMs` (default 300, range 50–2000). Context menu can **generate missing** (this folder), **generate missing (all subfolders)**, or **regenerate** strips — 20 frames sampled evenly via bundled ffmpeg into that same cache convention. **Generate missing** treats a strip as present only when all 20 frames exist and are non-empty; partial/interrupted strips are cleared and redone. Missing/partial strips fall back to the shell icon. Media allowlist admits the cache dir when frames resolve | Matches existing external strip caches; optional in-app generation into the same convention |
| D27 | **In-app image editor** (Filerobot): crop/rotate/flip/resize/finetune/filters/annotate — no heal/clone/AI. Entry from preview Edit button, context menu, and **Ctrl+E** (single editable image selected; ignored otherwise). **On NTFS:** Save writes tip ADS `VER_n` (max 4; higher = newer); default stream stays the pristine original; `VER_COUNT` = tip index. **Version Control** submenu when history exists: Commit / Revert; **Original** / **Version k** preview that stream only. Drop lives on the preview override banner (with **Show current**), not as duplicate submenu rows. MFE preview/slideshow/thumbs show the tip; Explorer still sees the default stream. **On non-NTFS:** Save overwrites the file body in place (no streams, no warning). **Copy/move NTFS→non-NTFS:** write tip bytes as the destination file (keep edits; drop original/history). Migrate legacy `userData/image-originals/` on ready | Non-destructive edits travel with the file on NTFS; elsewhere behave like a normal editor |
| D28 | **File-op progress** in the status bar for copy, move, rename, trash, permanent delete, relocate, video-preview generation, and **ZIP compress**. Main emits throttled `op-progress` (work units + optional `bytesDone`/`bytesTotal` for large file copies ≥8 MiB). Status bar layout: **fixed progress bar → title → counts → truncating current path → Cancel**. Determinate when advancing; **indeterminate** while `done === 0` or total unknown. Counts are **files** (a folder is not “1 of 1”): copy discovers files as each directory is listed; move counts a folder first (same-volume rename then jumps; cross-volume copies per file). Renderer also shows indeterminate busy after **1 s** if an awaited FS op is still running and main has not yet reported real progress. **Cancel** (`fs:cancelOp`) stops between items (and mid large-file stream / mid **7za** compress by killing the helper). End-of-op issue review (D18) opens **after** the pass; Cancel during the pass still surfaces queued issues | Honest feedback whenever the user is waiting; matches D15 honesty for search |
| D29 | **Search results use the normal file view** (`FileView`) — Details by default with a **search-only** sortable **Folder** column (containing path; not stacked under the name; not persisted / not in column picker), multi-select, preview, DnD, context menu. Search extras: **Open File Path** / **Open File in new tab** (in-app location). Banner above results. Not a separate results list. **Search is a tab history location** (WFE): starting a search pushes the folder; opening a folder from results pushes the search; Back/Forward restore folder or search (folder entries keep `scrollOffset`). Each tab keeps its own query/results; switching tabs or focusing another pane does not clear. Session persists query + scope (not rows). **Delete / move** prunes those paths from the live result list (and descendants of a removed folder) so hits do not linger like Explorer; a still-running walk will not resurrect them. | One interaction model; sort/filter by location like a real folder listing; Back returns to the search |
| D30 | **ZIP compress + extract + preview tree**: **Compress to ZIP file** via bundled **7za** (streams to disk; `%` progress; Cancel kills 7za). **Extract All…** still JSZip (sibling folder, zip-slip safe, per-file progress + Cancel). Selecting a `.zip` shows preview `kind: 'archive'` with an expand/collapse **contents tree** (not a navigable virtual folder — still deferred). Listing reads the **central directory only** (archive byte size irrelevant); trees cap ~4000 nodes. **`.unitypackage`**: same tree UI (`archiveFormat: 'unitypackage'`), streams gzip+tar and maps GUID `pathname` → Unity paths; **no Extract All** (Unity owns import). **Also list-only trees** for `.7z` (bundled 7za), `.rar` (WASM UnRAR), `.tar` / `.tar.gz` / `.tgz`, **`.apk`** (ZIP CD + AndroidManifest package/version), **`.msi`** (7za list + shell icon / VERSIONINFO when present), **`.iso`** / best-effort **`.img`** (UDF-preferred + ISO9660/Joliet walker — not 7za, which lacks ISO/UDF) — Extract All remains ZIP-only | Explorer zip workflow + inspect-without-extract; broader archive inspect |
| D31 | **Multi-pane file views** (Q-Dir-style): session `viewLayout` `1` \| `2` (side-by-side) \| `3` (wide top + two bottom) \| `4` (2×2). Each pane is a mini-explorer (own tree + files + nav) bound to at most one tab via `paneTabIds`. Folder tree collapse is **per pane** (`paneTreeCollapsed`; toggle on that pane’s toolbar, not the global chrome). Dragging a tab onto a pane **assigns/moves** it there; **Ctrl+drag** duplicates into the pane (same path/view; source pane keeps the original). Switching layout auto-fills empty slots from unassigned open tabs; leftover slots are drop targets (“Drop a tab here”). Focused pane syncs `activeTabId`; toolbar search box, preview, and keyboard nav target the focused pane. **Each pane’s tab keeps its own search overlay** so you can drag result files onto another tab/pane without the source list reverting to the folder. Shared preview; global tab bar. Persist layout + pane assignments + pane split ratios + per-pane tree collapse in `session.json`; named layouts (D25) snapshot the same. **Drop chrome**: full-pane / file-list background outline only when `viewLayout > 1` (single-pane has nowhere else to drop); folder row/cell targets still highlight in 1-pane | Side-by-side / grid workflows without multiple windows |
| D32 | **Tab chrome context menu** (right-click tab): Duplicate, Rename, Set/Change icon, Close. **Tab icons** are optional Lucide React glyphs (`icon.name` PascalCase + `icon.color` `#rrggbb`) persisted on the tab in `session.json` and named layouts (D25). **New tabs** get a default: unscoped / Computer → `Monitor` `#60a5fa`; drive as root → `HardDrive` `#94a3b8`; folder as root → `Folder` `#fbbf24`. Settings → Appearance **Show tab icons** (default on) hides all tab icons without clearing them. Picker can change or clear. Double-click title still renames | Color-code long-lived workspace tabs without inventing a custom icon pack |
| D33 | **In-pane video**: MP4/WebM/M4V play via `<video>` + byte-range `mfe-media`. MKV (and similar) remux/transcode to MP4 under `userData/video-remux/` when codecs allow. **`.avi` / `.divx` are strip-only** — no convert/player; show `!VIDTHUMB_CACHE` animation when present + **Open with default app**. Never write remux sidecars into browsed folders (D2) | AVI/DivX codecs aren’t Chromium-native; full re-encode is too heavy for preview |
| D34 | **Everything-parity search (hybrid)**: keep **folder roots** (walk + debounced FS watch). Optional **Index this drive** on fixed NTFS volumes via MFT/USN enum + USN monitor (fallback walk). Everything-inspired query language (AND/OR/NOT, `size:`/`dm:`/`ext:`/`path:`, macros, regex, …), as-you-type debounce, match-path/case/ww/regex toggles, unindexed `content:` with honesty banner, saved filters/bookmarks, **named Power Search designs** (`powerSearchSaved` — query/builder only; target folder vs indexed chosen at run), optional localhost HTTP API. Stay a file manager (D29 results UI); not an Everything UI clone. Replaces prior SEARCH.md non-goals that banned whole-disk opt-in and regex | Power-user search without mandatory whole-disk indexing |
| D35 | **`.chm` full preview**: decompile with Windows `hh.exe` into `userData/chm-preview/`; TOC from `.hhc`; topics over `mfe-media://chm/<hash>/…` in a sandboxed iframe (no scripts). Not a navigable virtual folder. Cap size/TOC; path traversal Locals omitted. Prefer Open (system HTML Help) when decompile fails | Inspect help files in-pane without LGPL CHM libs or `file://` |
| D36 | **`.ttf` font preview**: `kind: 'font'` — load allowlisted file over `mfe-media` (`font/ttf`) with `FontFace` / sample pangram; parse name table for Family / Full name / Version / Copyright. Does not install fonts into the OS. OTF / WOFF / WOFF2 deferred | Inspect typefaces in-pane without system install |
| D37 | **Slideshow / image categorizer** (gated chrome): Settings → **Slideshow** → **Enable slideshow UI** master switch. Slideshow settings section is always in the nav. When off: hide toolbar Start/Cache and folder **Start Slideshow** (and stop any active player). When on: those chrome items plus fullscreen player with auto/manual modes, categorizer map (`"Name", Keys.X, "path"`), commit deletes/categorizes only on stop. **Draw caption** frames the photo in a poster from ADS `Caption` when present (random entry; border/titles hashed from caption text) | Optional power-user chrome without removing settings; settings panel layout stays stable |
| D47 | **Slideshow numpad crop** (manual mode): **2/4/6/8** trim bottom/left/right/top (5% default; Shift 2.5%, Ctrl 1%, Shift+Ctrl 0.5%); steps accumulate from pristine `$DATA`; **Enter** / **Numpad0** save (single Sharp encode) or resume autoplay when not cropping; **Esc** / **Numpad5** cancel crop; other keys blocked while cropping | Quick trim during categorizer sessions without opening the full editor |
| D38 | **NTFS Alternate Data Streams**: one main-process module (`adsWin32`, Trinet.Core.IO.Ntfs / ADS.cs parity via koffi `BackupRead` / stream paths). Opt-in async Details column **Alternate streams** (comma-separated names; files + dirs). Opt-in **stream-value** columns (`adsField:<name>`, header = stream name or user label, cell = ADS Manager text preview) listed under **Stream values** (names found on the current listing only; **...** is the only catalog write; **Clear** wipes saved stream columns). Manager dialog + `ads:*` IPC for list/read/write/delete/copy/`listNamesMany`; soft-fail off win32 / access denied. Do not enumerate ADS on every `fs:list`. Copy/move rely on Win32 preserving streams; `ads:copy` is explicit tooling. Zone.Identifier etc. listed without special UI. ADS write/delete restores host **FileBasicInfo** (Creation/Access/Write/**Change**) so metadata streams do not look like a file change to sync tools | Power-user NTFS tooling without slowing normal listing |
| D39 | **Compiled file lists slideshow**: Category folders hold `.dat` (body = source folders to crawl; Index/Count = jpg/png list after Update Lists; `|=>` ignored during Update) and `.txt` (folders and/or nested `.dat`/`.txt` refs with `|=>` counts; cycles break; **never** Index ADS — always expand from body at play; **Count** is rewritten to the expanded image total after that expand). **Update Lists** skips `!!Lists`, processes **`.dat` only** one at a time (no in-memory folder-scan cache) with cancelable progress UI. **Validate Lists** reports missing folders / nested list refs. Live compiled play is a **virtual playlist** (segments + index, max `2^31-1`; Uint32/Feistel random; no flat path×count array). `!!Lists` tab + last.txt resume; ±/# rebuild virtual playlist in main. Folder Start/Cache unchanged (100k cap) | Fast large-library slideshow without live folder scans on `.dat` or multi‑GB string arrays |
| D40 | **In-app Power Rename** (PowerToys-inspired): context **Power Rename…** for ≥1 selected files/folders. Search/replace with optional regex, match-all, case sensitivity; apply to filename+ext / name only / extension only; live preview + per-item checkboxes. Apply uses `fs:rename`; name clashes join the D18 review (Skip / Keep both / Replace / Keep most recent). Dialog session Undo + one `power-rename` undo-stack entry. **v1 scope:** rename selected basenames only — no enumerate counters, EXIF/date tokens, Boost regex, or recurse-into-selected-folders | Multi-file rename without PowerToys; keep v1 predictable and testable |
| D41 | **Context menu customization**: Settings → **Context menu** with **Built-in** checklist (`hiddenBuiltins`) plus drag-reorder / separator layout (`builtinLayout` — built-ins and enabled Discover verbs), **Discover** (opt-in scan of static HKCR shell verbs; catalog + `enabledIds` persist; tick enables on the live menu and adds a tinted orderable row under Built-in — no checkbox there; Rescan refreshes catalog and keeps ticks; no COM shell extensions), plus separate **Custom (files)** / **Custom (folders)** lists of hand-edited external commands (label, absolute exe with `%ENV%`, argv template `{path}`/`{paths}`/`{dir}`/`{name}`, match all or by extension). Custom + enabled Discover verbs shown when the whole selection is uniformly files or folders and matches. Launch via `shell:exec` (env expand → absolute path → detached spawn, no shell). Cap 30 custom/list; cap 60 enabled Discover. Never host Explorer `IContextMenu` / ContextMenuHandlers | Trim unused verbs + Photoshop/VLC-style Open with without Explorer verb dump; Discover is enable+order, not auto-dump |
| D42 | **Item check boxes** (optional): Settings → Behavior → **Item check boxes** (`itemCheckboxes`, default off). When on, show Explorer-style checkboxes before the name/icon in list/details and on icon cells (hover/selected). Checkbox click toggles that item like Ctrl+click without clearing the rest | Classic Explorer Folder Options parity without forcing the UI on everyone |
| D43 | **File Tools** context submenu: **Copy To…** / **Move To…** for ≥1 selected items (files and/or folders) open an in-app target-folder picker (drive tree + path field + Browse… + optional open-after). Transfer reuses `performTransfer` (end-of-op issue review, D18). **Change Icon…** for a single folder: pick a `.ico`, copy to `Folder.ico`, write `desktop.ini`, set folder Read-only + Hidden/System on ico/ini, clear shell icon cache (`fs:setFolderIcon`). Hideable via D41 `file-tools` | FileMenu Tools–style relocate without Explorer shell extensions; folder icons stay portable with the folder |
| D44 | **Network neighborhood**: tree **Network** section below Drives only when at least one **reachable** host is listed. **Offline Network PCs are hidden** (unlike disconnected mapped letters under Drives). Local PC filtered out unless **Settings → Network → Show local computer** (`networkDiscovery.showLocalComputer`, default **false**). Remembered names in `userData/network-hosts.json` are probe targets only — worker probes them first (TCP 445 + `NetShareEnum`), then Shell Network + ARP (**~20s budget**); only answering hosts enter the tree. Empty discovery does **not** flash an empty Network section. **Settings → Network**: discovery **auto** (default, interval 1–60 min, default **5**) or **manual**; Map / Disconnect / Discover now. Skips legacy `NetServerEnum`. Opening `\\server` / listing shares remembers the host. Expand host → `NetShareEnum`. Soft-fail empty LAN. D41: `map-network-drive`, `disconnect-network-drive`, `network-refresh` | Configurable rediscovery without permanent “discovering…”; ditch WFE for LAN + mapped drives |
| D45 | **Settings export / import** (Settings → About): portable JSON (`format: myfileexplorer-settings`) includes the **full** `settingsSchema` document — theme, named layouts, folder views, slideshow, **context menu (D41: `hiddenBuiltins`, `builtinLayout`, Custom files/folders, Discover `discovered.verbs` + `enabledIds`)**, **networkDiscovery**, **remoteRepos.enabled**, … — plus remembered Network hosts **and remote connection metadata** (`remoteConnections[]`, no passwords). **Excludes** main `window-state.json` and dialog geometry (`propertiesBounds`, `adsManagerBounds`, `usnManagerBounds`, `powerRenameBounds`, `remoteConnectionBounds`, `compiledListsWindowBounds`, `previewWindowBounds`). **Rule:** every new setting must be added to `settingsSchema` (or a nested schema already under it) so export/import round-trips it — no separate allowlist. Sidecar lists that are not settings (Network hosts, remote connections) ride along as explicit envelope fields. Import replaces settings (confirm); open tabs/`session.json` unchanged — apply a named layout to restore a workspace. Also accepts a raw `settings.json` body (sidecars unchanged) | Move prefs (incl. context-menu customization) to a new PC / after OS reinstall without copying window position |
| D46 | **Remote repositories** (FTP / FTPS / SFTP): Settings → Remote repositories → **Enable** (`remoteRepos.enabled`, default **false**). When on: toolbar Select / Add / Edit / Rename / Delete / Connect / Disconnect; tree **Remote repositories** section only if ≥1 saved connection. Locations use `mfe-remote://{id}/posix/path`. Passwords in Electron `safeStorage`; metadata in `remote-connections.json`. Ops: list, mkdir, rename, permanent delete, upload/download + local↔remote copy/move; Open/preview stage under `userData` scratch. FTP/FTPS session ops serialized per connection. Busy modals for connect / folder open / file open. No Recycle Bin, watches, or search index over remotes. Cleartext FTP requires insecure ack. Spec: [REMOTE_FTP.md](REMOTE_FTP.md) | Deploy/sync without leaving the file manager; keep Windows-local as primary |
| D48 | **3D mesh preview** (`.obj` / `.fbx` / `.3ds`): `kind: 'model3d'` — WebGL orbit view in the renderer via code-split `three.js` loaders. Files over `mfe-media` (D7); sibling MTL/textures from the model folder + one subfolder level. OBJ counts from a text scan; no animation player / no writing sidecars (D2). Skip WebGL above 96 MiB. Canvas fills leftover preview-pane height | Typical Unity project meshes without a DCC app |
| D49 | **Remote listing memory cache**: session-only renderer LRU (~24 folders; skip &gt;20 000 entries) for UNC, mapped remote letters, and `mfe-remote://`. Navigate paints the last listing immediately and revalidates with `fs:list` in the background. Drop on F5, in-app mutations, and `fs-changed` for that folder. Never persist listings to disk; never cache local NTFS | NAS / UNC re-open should feel like Explorer’s second visit without serving stale as truth |
| D50 | **Media metadata** (opt-in, `mediaMetadata.enabled` default **false**): Settings → Media Metadata. When on: context **Media Metadata** on folders and video files only (Extract from Plex / Download from Internet / Update / Clear / Consolidate subtitles / Change cover). Change cover lists Plex library posters and TMDB posters for that title, largest pixel size first. Folders recurse all videos. Extract/Download skip items that already have streams; Update refreshes existing from their stored source and extracts missing from Plex. JSON + cover stored as NTFS ADS `media_metadata` / `media_metadata_thumbnail` on the file or folder (D2 — no sidecars). Writing metadata also sets `media_metadata_container` on the containing folder; toolbar Watched/Genre filters appear only when that stream is present. `watched` is a user field on the JSON (preserved across extract/update). TMDB/OMDb quota stops the batch and shows an alert. A title miss opens **Search as** (filename stem, editable, sent as typed, or a TMDB / IMDb URL) and retries until Search succeeds or Cancel. **Which title?** can **Search as…** the same way. Preview: title + cover above the video (`coverHeightPx`, default 120); Media / File tabs under the player when both movie/TV details and extracted file metadata exist; click the cover for a fullscreen view of the stored image. Show poster + card live on the **show folder** (XL icons only hides the folder name when a cover is present); episode files store episode JSON only, keep `!VIDTHUMB_CACHE` icons, and in icon/thumbnail views list as `SxxExx` when stored (episode title under the code when present; `showEpisodeIconLabels` default true, off = filename; Details/List stay the filename; tooltip is always the filename). `media_metadata_container` is set on the library parent of show folders. Movie vs show is auto-detected (dialog only if unclear). Internet download asks **Which title?** only when several exact-title hits remain after the year (Plex extract does not — one library item). A folder of CD-split movie parts (`[Part 1]`, `CD1`) is the movie (parts are not written). Covers used as icon thumbs when present. Plex via local HTTP API (token from Preferences.xml) or library SQLite; internet via TMDB / OMDb API keys. Spec: [MEDIA_METADATA.md](MEDIA_METADATA.md) | Movie/TV libraries need title/cover without polluting folders; off by default so a first install stays a file manager |
| D52 | **NTFS USN journal manager** (drive Properties only): **USN…** next to **Windows Properties…** opens an in-app manager. Query / recent records via existing koffi `DeviceIoControl` (`FSCTL_QUERY_USN_JOURNAL` / `READ`). Enable / resize = `FSCTL_CREATE_USN_JOURNAL` (defaults 64 MiB max / 8 MiB delta); disable = delete journal; clear = delete + create. Drive-root paths only — never on file/folder Properties. Native create/delete usually fails from a non-elevated process (UAC split token) even if the user is an admin — Enable then offers **Retry as administrator** (`fsutil usn` + UAC `runas`). After a first-time Enable (journal was not already active), main writes then deletes a unique `testing USN *.txt` on that volume so Recent immediately shows Create + Delete. VeraCrypt / other virtual NTFS volumes are treated as normal NTFS (encryption is below the FS). Disable/clear are explicit **Delete journal** actions: Windows volume-wide MFT USN reset (ERROR 1178 while in progress) — hours on multi-million-file / encrypted volumes, cannot be cancelled; two-step confirm; UI shows Deleting… with elapsed time and does not hammer query I/O. Soft-fail off win32 / non-NTFS. Last sizes on `settingsSchema`; dialog geometry stripped on export (D45). Does not run journal ops on `fs:list`. Search **Index this drive** still assumes a journal when present (D34) and does not auto-create one | Power-user NTFS journal control without leaving the file manager; indexer stays read-only |
| D51 | **Universal local script runner + optional AI authoring.** Isolated `script:*` / `ai:*` IPC (not `shell:exec`). Runner spawns argv arrays with `--root` / `--input-list` (temp UTF-8 manifest), live output, cancel. **Python 3.x only — Python 2.x is not supported.** Library under `userData/scripts/` (D2). Context **Scripts** submenu (D41 builtin `scripts`) — a saved script is a first-class verb; that is how capabilities grow without a new release. Settings → **Scripting and AI**: **Enable scripting** (off by default — hides toolbar / context Scripts). Optional AI: OpenAI-compatible providers; keys in `safeStorage`. Model pickers are dropdowns from `GET /v1/models` (cached on the provider; OpenAI catalogs hide embeddings/TTS/image). **AI may write code; it never receives paths, listings, or file bytes.** Saved scripts rerun with no AI. Dry-run, declared deps (copy install command, never silent install), Ask AI to Fix with explicit confirm, `.mfescript` import/export, external file refs, revert after AI modify, optional categories. After a real **Run** (or Stop), refresh the visible file list and folder tree like F5; dry-run does not. Not a plugin SDK / scheduler / File Automator. Guide: [SCRIPTS.md](SCRIPTS.md) | Users need repeatable local automation without giving a model their files; keep D41 as “launch an exe” |

---

## Deferred (not v1 decisions)

- Archive browsing as virtual folders (open `.zip` as a navigable folder)
- Plugin system for third-party context verbs
- Cloud provider namespace integration
- **Remote FTP/SFTP repositories** — shipped as opt-in D46; see [REMOTE_FTP.md](REMOTE_FTP.md). Not local-drive parity.
